/*
 * Exercício 15 — Set & Map
 * -------------------------
 * Objetivo: praticar remoção de duplicatas com Set e uso de Map para contagens.
 */

function removerDuplicatas(lista) {
  // TODO: retorne nova lista sem valores repetidos usando Set.
  return [];
}

function contarOcorrencias(lista) {
  // TODO: use Map para contar ocorrências de cada item e retorne o Map.
  return new Map();
}

function temValor(map, chave) {
  // TODO: cheque se chave existe usando map.has.
  return false;
}

module.exports = {
  removerDuplicatas,
  contarOcorrencias,
  temValor,
};

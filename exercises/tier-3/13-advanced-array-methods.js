/*
 * Exercício 13 — Métodos Avançados de Array
 * -----------------------------------------
 * Objetivo: praticar some, every, sort, flat e flatMap.
 */

function temMaiorQue(lista, limite) {
  // TODO: retorne true se algum número for maior que limite (some).
  return false;
}

function todosAcimaDe(lista, limite) {
  // TODO: retorne true se todos forem maiores que limite (every).
  return false;
}

function ordenarPorIdade(usuarios) {
  // TODO: retorne NOVO array ordenado por idade crescente (use spread + sort para não mutar original).
  return [];
}

function achatarNiveis(valores, profundidade = 1) {
  // TODO: use flat para achatar o array até a profundidade.
  return [];
}

function duplicarEIntercalar(valores) {
  // TODO: use flatMap para retornar [valor, valor*2] para cada item.
  return [];
}

module.exports = {
  temMaiorQue,
  todosAcimaDe,
  ordenarPorIdade,
  achatarNiveis,
  duplicarEIntercalar,
};

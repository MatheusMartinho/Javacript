/*
 * Exercício 05 — Arrays Essenciais
 * ---------------------------------
 * Objetivo: dominar métodos map, filter, find, reduce e includes.
 *
 * Tarefas:
 * 1. Complete `dobrarNumeros` retornando um novo array com cada número * 2.
 * 2. Faça `filtrarPares` devolver apenas números pares.
 * 3. Implemente `primeiroMaiorQue` recebendo um limiar e retornando o primeiro número acima dele.
 * 4. Faça `somarNumeros` usar reduce para somar todos valores (valor inicial 0).
 * 5. Complete `contemValor` verificando com includes.
 * 6. (Extra) Implemente `estatisticas` retornando um objeto { soma, media, maior } usando reduce.
 */

function dobrarNumeros(numeros) {
  // TODO
  return [];
}

function filtrarPares(numeros) {
  // TODO
  return [];
}

function primeiroMaiorQue(numeros, limite) {
  // TODO
  return undefined;
}

function somarNumeros(numeros) {
  // TODO
  return 0;
}

function contemValor(numeros, valor) {
  // TODO
  return false;
}

function estatisticas(numeros) {
  // TODO: use reduce para calcular soma, média e maior.
  return { soma: 0, media: 0, maior: null };
}

module.exports = {
  dobrarNumeros,
  filtrarPares,
  primeiroMaiorQue,
  somarNumeros,
  contemValor,
  estatisticas,
};
